ruta (origen, origent, destino, contador)
{
	print(origent);
	if(origent == destino){
		print("sale") retun;
	}
	else if(origent == origen){
		print("no llegó") return;
	}
	else{
		for(i=0; i<9;i++){
			if(arreglo[origent,i]==1){
				ruta(origen, i, destino, contador+1);
			}
		}
	}
}